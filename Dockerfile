# Используйте официальный образ Python в качестве базового
FROM python:3.12-alpine

# Установите рабочую директорию в контейнере
WORKDIR /app

# Скопируйте файлы вашего приложения в рабочую директорию контейнера
COPY . .

# Установите зависимости вашего приложения
RUN pip install --no-cache-dir -r requirements.txt

# Откройте порт 50051 для gRPC сервера
EXPOSE 50051

# Команда для запуска вашего gRPC сервера
CMD ["python", "server.py"]
